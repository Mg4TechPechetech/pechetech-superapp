name: pechetech_superapp
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  cloud_firestore: ^6.3.0
  firebase_auth: ^6.4.0
  firebase_auth_platform_interface: ^8.1.9
  firebase_core: ^4.7.0
  firebase_storage: ^13.3.0
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.4
  google_fonts: ^6.1.0
  google_sign_in: ^7.2.0
  http: ^1.6.0
  image_cropper: ^12.2.1
  image_picker: ^1.2.2
  intl: ^0.19.0
  universal_io: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.6.4
  build_runner: ^2.15.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
